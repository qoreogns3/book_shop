<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.book_shop.member.mapper.MemberMapper">
    <resultMap id="member" type="com.green.book_shop.member.dto.MemberDTO">
        <id     column="MEM_EMAIL"      property="memEmail" />
        <result column="MEM_PW"         property="memPw" />
        <result column="MEM_NAME"       property="memName" />
        <result column="MEM_TEL"        property="memTel" />
        <result column="MEM_ADDR"       property="memAddr" />
        <result column="ADDR_DETAIL"    property="addrDetail" />
        <result column="IS_USING"       property="isUsing" />
        <result column="MEM_ROLE"       property="memRole" />
        <result column="JOIN_DATE"      property="joinDate" />
    </resultMap>


    <insert id="join">
        INSERT INTO SHOP_MEMBER(
            MEM_EMAIL
            , MEM_NAME
            , MEM_PW
            , MEM_TEL
            , MEM_ADDR
            , ADDR_DETAIL
        ) VALUES (
            #{memEmail}
            , #{memName}
            , #{memPw}
            , #{memTel}
            , #{memAddr}
            , #{addrDetail}
        )
    </insert>

    <!--이메일 중복 조회 쿼리-->
    <select id="checkId" resultMap="member">
        SELECT MEM_EMAIL
        FROM SHOP_MEMBER
        WHERE MEM_EMAIL = #{memEmail}
    </select>

    <!-- 로그인 쿼리 -->
    <select id="login" resultMap="member">
        SELECT MEM_EMAIL
            , MEM_NAME
            , MEM_ROLE
        FROM SHOP_MEMBER
        WHERE MEM_EMAIL = #{memEmail}
        AND MEM_PW = #{memPw}
    </select>
    <select id="loginEmail" resultMap="member">
        SELECT MEM_EMAIL
            , MEM_NAME
            , MEM_ROLE
        FROM SHOP_MEMBER
        WHERE MEM_EMAIL = #{memEmail}
    </select>


</mapper>































