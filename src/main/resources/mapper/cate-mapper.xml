<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.book_shop.cate.mapper.CateMapper">
    <resultMap id="cate" type="com.green.book_shop.cate.dto.CateDTO">
        <id property="cateNum"               column="CATE_NUM"/>
        <result property="cateName"         column="CATE_NAME"/>
    </resultMap>

    <select id="getCateName" resultMap="cate">
        SELECT CATE_NAME
            , CATE_NUM
        FROM BOOK_CATEGORY
        ORDER BY CATE_NUM ASC
    </select>


</mapper>































